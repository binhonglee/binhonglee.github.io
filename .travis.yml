language: node_js
os:
  - linux
  - osx

node_js:
  - "node"
  - "stable"

install:
  - npm install
  - npm install -g codecov

script:
  - npm test
  - codecov -t $CODECOV_TOKEN
  - npm run build
  - cp -r images dist

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  target-branch: master
  local-dir: dist
  on:
    branch: dev
